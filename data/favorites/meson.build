favorites_dist = [
	'.order',
	'desktop.desktop',
	'home.desktop',
	'root.desktop',
	'tmp.desktop'
]

install_data(favorites_dist,
	install_dir: join_paths(dir_data, 'enlightenment/data/favorites')
)
